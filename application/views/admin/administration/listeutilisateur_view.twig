{% extends "/layout/admin-layout.twig" %}

{% block title %} {{parent()}} - login administrateur{% endblock title %}

{% block container %}

    {% block content %}
            <div class="span12">
                <div class="well">
                    <ul class="nav nav-pills" style="border-bottom: 1px solid #cccccc">
                        <li class="pull-left"><h4>{{titre}}</h4></li>
                         {%if login != NULL%}
                        <li class="pull-right"><h4>Voici le login recherché : {{login}}</h4></li>
                        {%endif%}
                   </ul>
                
                    <div class="table table-striped table-hover ">
                       {{ table }} 
                    </div>
                    <div class="pagination">
                      {{pagination}}
                    </div>
                        
                </div>
            </div>
       {% endblock content %}
   
{% endblock container %}