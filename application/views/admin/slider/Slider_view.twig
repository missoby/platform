{% extends "/layout/admin-layout.twig" %}

{% block title %} {{parent()}} - login administrateur{% endblock title %}


{% block container %}
    <div class="row">
        {% block content %}
            <div class="span12">
            <div class="well">
                {#---Logo---#}
                <div class="span3 offset4">
                    <a href=""><img src="{{base_url()}}assets/img/logo.png" alt="E-COMMERCE"/></a>
                </div>
           
                     <div >
                                {%if nbPhoto == 0 %}
                                    <p>Vous n'avez aucune photo pour le slider</p>
                                     <a class="btn" href='/admin/admin/addslider'>Ajouter une photo pour slider</a>
                                {%elseif nbPhoto > 8 %}
                                  <p>Vous avez dépasser le nombre permis de photo (5)</p> 
                                   {%else%}
                                   <a class="btn" href='/admin/admin/addslider'>Ajouter une photo pour slider</a>
                                   
                                 {%endif%}
                               
                            </div>
                       <div >
                    {%for var in slider.result() %}
                            <p>
                     <img src="{{ finalpath }}{{var.photo}}" alt="" height="159" class="img-polaroid span3">     
                     <a href='/admin/admin/deleteSlider/{{var.id}}'>Supprimer photo</a>  
                     </p>
            {%endfor%}
                    </div>
                     </div>
            </div>
        {% endblock content %}
    </div>
{% endblock container %}
